{
  "name": "dev-chef-backend",
  "version": "1.0.0",
  "description": "Backend do Dev Chef - Assistente de Receitas",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build:frontend": "cd interfaces-receitas && npm install && npm run build",
    "postinstall": "npm run build:frontend"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "openai": "^4.20.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}